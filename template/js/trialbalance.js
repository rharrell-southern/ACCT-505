/* Trial Balance Controller
 * 
 * The trial balance controller is used to setup and handle Trial Balance sheets
 */
var TrialBalance = (function($) {
	var solutions;
	
	return {
		'setSolutions': function() {
			
		}
	};
})(jQuery);
